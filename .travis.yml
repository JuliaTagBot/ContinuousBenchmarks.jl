language: julia
julia:
  - 1.0
os:
  - linux
  - osx
matrix:
    fast_finish: true
    allow_failures:
       - os: osx

notifications:
    email: false
script:
  - julia --check-bounds=yes -e 'using Pkg;
                                 Pkg.clone(pwd()); 
                                 Pkg.build("TuringBenchmarks"); 
                                 Pkg.test("TuringBenchmarks"; coverage=true)'
after_success:
  - julia -e 'cd(Pkg.dir("TuringBenchmarks")); Pkg.add("Coverage");
              using Coverage; Coveralls.submit(process_folder())'
  - julia -e 'using Pkg; Pkg.add("Documenter")'
  - julia -e 'using Pkg, TuringBenchmarks;
              cd(joinpath(dirname(pathof(TuringBenchmarks)), ".."));
              include(joinpath("docs", "make.jl"))'
notifications:
    email: false

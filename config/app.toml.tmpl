[server]
http_ip = "0.0.0.0"
http_port = 5900
stop_file = "config/stop-server"

[github]
# for App
app_id = 1000 
priv_pem = "config/xxxx.pem"
secret = "the webhook secret"

# for App Owner
user = "username"
email = "user_email"
token = "token"
